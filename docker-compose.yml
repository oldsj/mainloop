services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: mainloop-backend
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - GOOGLE_APPLICATION_CREDENTIALS=/secrets/gcp-credentials.json
      - CLAUDE_WORKSPACE=/workspace
    volumes:
      - ${GCP_CREDENTIALS_PATH:-./secrets/gcp-credentials.json}:/secrets/gcp-credentials.json:ro
      - claude-workdir:/workspace
    develop:
      watch:
        - action: rebuild
          path: ./backend
          ignore:
            - .venv
            - __pycache__
        - action: rebuild
          path: ./models
          ignore:
            - .venv
            - __pycache__

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: mainloop-frontend
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    environment:
      - VITE_API_URL=http://localhost:8000
      - USE_NODE_ADAPTER=true
    depends_on:
      - backend
    develop:
      watch:
        - action: rebuild
          path: ./frontend
          ignore:
            - node_modules
            - build
            - .svelte-kit
        - action: rebuild
          path: ./packages/ui
          ignore:
            - node_modules

  claude-agent:
    build:
      context: ./claude-agent
      dockerfile: Dockerfile
    container_name: mainloop-claude-agent
    ports:
      - "8001:8001"  # Expose for debugging
    env_file:
      - .env
    volumes:
      - claude-workdir:/workspace

volumes:
  claude-workdir:
